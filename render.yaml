services:
  - type: web
    name: emaara-interiors
    runtime: docker
    plan: free
    buildCommand: echo "No build required"
    startCommand: php -S 0.0.0.0:$PORT -t .
    envVars:
      - key: PHP_VERSION
        value: 8.2
